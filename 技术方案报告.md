# Qwen3.5-2B 多模态推理软硬件协同优化技术方案报告

## 摘要

本方案面向 PPU-ZW810E 上的 Qwen3.5-2B 单图多选题推理，对视觉编码、语言模型 prefill、逐 token decode、缓存管理和输出选择进行协同优化。模型采用 BF16、batch size 1 和 greedy decoding，重点减少细粒度 kernel 启动、重复显存访问、动态缓存分配和 CPU 提交开销。

当前实现由 `evaluation_wrapper.py`、`qwen35_fused/integration.py`、`qwen35_fused/kernels.py` 和 `qwen35_fused/graph.py` 组成，覆盖 18 层 DeltaNet、6 层 Full Attention、24 层 MLP 和 24 层 Vision Transformer。最终在中英文公开集共 8058 个样本上的平均 TTFT 从 144.009 ms 降至 25.861 ms，降低 82.04%；平均解码吞吐从 55.804 token/s 提升至 241.010 token/s，为基线的 4.319 倍；完整评测耗时从 6537.914 s 降至 1739.350 s，降低 73.40%。双语合并准确率由 81.9186% 变为 81.8565%，变化为 -0.0621 个百分点。

## 1. VLM 应用场景

### 1.1 任务与指标

目标场景是基于图片、问题和 A/B/C/D 候选项进行多模态理解并生成唯一选项答案。评测入口固定为 `VLMModel.generate_with_metrics`，主要指标为：

- Accuracy：解析出的选项与标准答案一致的比例；
- TTFT：模型推理开始到第一个生成 token 的时间；
- Throughput：首 token 之后的平均解码吞吐，公开脚本按 `(token_count - 1) / (elapsed - TTFT)` 逐题计算后取算术平均；
- 完整运行时间：包含模型加载、warmup、图片解码、预处理、同步和全部样本推理的 benchmark wall time。

公开评测固定 `max_new_tokens=256`、`temperature=0.0`、`top_p=1.0`，优化路径据此采用 batch size 1 的 BF16 greedy inference。

### 1.2 模型计算特征

Qwen3.5-2B 是视觉编码器与混合语言主干组成的 VLM。当前 checkpoint 的活动参数量为 2,213,241,664，其中语言模型约 18.82 亿参数、视觉塔约 3.31 亿参数。关键结构如下：

| 模块 | 当前模型结构 | 主要计算与状态 |
|---|---|---|
| Vision Transformer | 24 层，hidden size 1024，16 heads | Patch embedding、位置插值、QKV/RoPE、Attention、MLP、LayerNorm |
| Language Model | 24 层，hidden size 2048 | 以 `linear, linear, linear, full` 重复 6 次 |
| DeltaNet | 18 层，16 heads，head dim 128 | depthwise causal conv、gated delta recurrence、BF16 conv state、FP32 recurrent state |
| Full Attention | 6 层，8 query heads、2 KV heads、head dim 256 | Q/K norm、M-RoPE、BF16 KV cache、attention gate |
| MLP | 24 层，intermediate size 6144 | gate/up projection、SwiGLU、down projection |
| LM head | vocab size 248320 | `[1,2048] × [2048,248320]` 后执行 greedy top-1 |

推理数据流为：

```text
图片 + 问题 + 候选项
        ↓
Processor / Chat Template / Tokenization
        ↓
Vision Encoder → 图像 token
        ↓
多模态位置编码 + Language Prefill → 第一个 token
        ↓
StaticCache + Decode Graph → 后续 token
        ↓
LM-head Top-1 → A/B/C/D 答案解析
```

### 1.3 基线瓶颈

原始 Transformers BF16 eager 路径包含大量短 kernel：Vision 每层重复执行 RoPE、布局变换和 norm；DeltaNet prefill 由 Python/chunk 递推触发大量 clone、copy 和 reduction；batch size 1 decode 则存在密集的小算子提交。基线 decode 的 device kernel 时间约 7.99 ms/token，端到端耗时为 22.27 ms/token，其余时间主要消耗在 host launch、设备调度、框架执行和同步。优化因此采用“算子融合 + 静态地址 + 图执行”的组合方案。

## 2. 软硬件协同设计方案

### 2.1 目标平台与软件栈

当前实现的验证环境如下：

| 项目 | 配置 |
|---|---|
| 加速卡 | PPU-ZW810E |
| Python | 3.12 |
| PyTorch | 2.9.0 |
| Transformers | 5.15.0 |
| Torchvision | 0.24.0 |
| 数据类型 | BF16 权重/激活，关键归约与 Delta recurrent state 使用 FP32 |
| Triton | PPU SDK 定制版 `3.5.0+git4328cd8b` |

PPU 通过 CUDA-compatible 接口接入 PyTorch。定制 kernel 使用 PPU Triton 实现，图执行用于降低 host 提交成本。wrapper 优先加载仓库中的 `triton/` 预编译缓存，缓存不存在时使用运行时编译；运行环境使用 PPU SDK 提供的 Triton 版本。

### 2.2 总体架构

方案按模型加载、prefill 和 decode 三个阶段设计：

```text
模型加载阶段
  权重拼接/对齐 → 注册 packed buffer → 释放被替代的原 projection 权重

请求准备阶段
  CPU Processor → 预计算 Vision 插值/位置/序列元数据 → 搬移到 PPU

Prefill 阶段
  Vision 融合路径 → Text 融合路径 → StaticCache 写入 → LM-head 分块 top-1

Decode 阶段
  选择长度 bucket → 重置并复用 StaticCache → 更新静态 token/position buffer
  → replay GreedyDecodeGraph → 设备端 top-1/token feedback/position increment
```

设计原则包括：

1. 针对固定模型 shape 选择 tile、warp、padding 和 prefill/decode specialization；
2. 归约与 recurrent state 使用 FP32，权重、激活和主要缓存使用 BF16；
3. 合并 projection 和相邻逐元素操作，并以固定地址的 StaticCache 支撑 decode graph。

### 2.3 模块与代码边界

| 文件 | 职责 |
|---|---|
| `evaluation_wrapper.py` | 保持评测接口，完成模型加载、输入准备、长度分桶、StaticCache/Graph 复用和计时 |
| `qwen35_fused/integration.py` | 权重 packing、模块 monkey patch、Text/Vision 融合前向以及缓存接入 |
| `qwen35_fused/kernels.py` | PPU Triton kernel 及其 PyTorch tensor wrapper |
| `qwen35_fused/graph.py` | greedy decode graph 捕获、静态输入更新和 replay |
| `precompile_triton.py` | 为生产 shape 预编译 Triton specialization |
| `qwen35_fused/validate_kernels.py` | 单算子数值一致性和性能验证 |

默认配置用于 `eval()`、PPU、BF16 连续张量和 batch size 1。调试其他 dtype 时可关闭权重释放或整体融合。

## 3. 优化方法设计

优化方法按作用层级分为三类：投影合并在模型执行图层面减少矩阵乘次数；Kernel 优化通过算子融合、分块和 shape specialization 提高设备执行效率；系统优化通过固定地址、缓存复用和图执行降低请求级分配与 CPU 提交开销。三类优化分别解决计算冗余、设备执行效率和运行时调度问题。

### 3.1 投影合并

投影合并在模型加载后一次性完成。原模型中，共享同一输入的多个 projection 分别读取 hidden state 并启动独立 GEMM/GEMV；当前实现沿输出维拼接其权重，将多次 projection 改为一次矩阵乘，再由后续 kernel 按约定区间读取各部分结果。

| 模块 | 合并前 | 合并后 | 适用层数 |
|---|---|---|---:|
| DeltaNet | Q、K、V、Z、A、B 分别投影 | packed QKV/Z/A/B，输出宽度按 128 行对齐到 8320 | 18 |
| MLP | gate、up 分别投影 | `[12288, 2048]` packed gate/up weight | 24 |
| Full Attention | 包含 Q/gate 的 q projection 与 K、V projection | 输出宽度 5120 的 packed QGKV | 6 |

以 MLP 为例，原路径分别计算 `hidden × W_gate` 和 `hidden × W_up`，需要读取两次 hidden state、启动两次矩阵乘并生成两个输出；合并后计算一次 `hidden × concat(W_gate, W_up)`，同时得到 gate 和 up。DeltaNet 与 Full Attention 使用相同思路，将一次 packed projection 的输出直接交给后续融合 kernel。

Weight packing 会新建 packed tensor。拼接结果经 `detach()` 后注册为推理 buffer，不再纳入训练参数和 autograd；随后从模块中注销已被替代的 projection parameter，使其显存可以释放。这样模型只保留实际执行所需的 packed weight，融合后新增 allocated memory 从约 1.947 GB 降至约 7.08 MB。

### 3.2 Kernel 优化

Kernel 优化围绕矩阵乘输出处理、状态递推、残差归一化和结果选择四类计算展开，具体包括相邻算子融合、固定 shape specialization、计算分块、归约复用和状态原位更新。算子融合用于减少中间张量读写，其余方法用于提高并行效率或移除框架层调度开销。

| 优化对象 | Kernel 内完成的处理 | 主要收益 |
|---|---|---|
| Text/Vision block 边界 | residual add、RMSNorm/LayerNorm、gate 或逐元素激活 | 减少 residual 和 norm 中间量读写 |
| DeltaNet 卷积 | causal conv1d、SiLU、conv state 更新 | 消除独立激活和状态 copy |
| DeltaNet 递推 | Q/K 归一化因子、衰减/更新门、gated-delta recurrence、state 更新 | 消除 Python chunk loop 和细粒度 reduction |
| Full Attention 投影后处理 | Q/gate/K/V 拆分、Q/K Norm、M-RoPE、布局转换、KV cache 写入 | 避免多个临时张量和 `index_copy_` |
| Vision 投影后处理 | packed QKV 布局转换、Q/K RoPE、Q/K/V 输出 | 合并每层重复的小算子 |
| MLP 激活 | SiLU 与 gate/up 逐元素乘；decode 时进一步与 GEMV 合并 | 避免 gate/up 中间张量落盘 |
| LM Head | 分块 GEMV、局部 Top-1、全局 Top-1 | 避免完整 logits 的写入和再次读取 |

Text 24 层和 Vision 24 层的执行循环均重写了 residual/norm 边界。通用 Triton kernel 覆盖 RMSNorm、residual-add + RMSNorm、gated RMSNorm + SiLU gate、Vision LayerNorm、Vision residual-add + LayerNorm、SiLU × up、sigmoid gate × attention output，以及 Vision 位置插值求和 + hidden add。归约中的 RMS、均值和方差使用 FP32；需要保持原计算语义的位置显式保留 BF16 舍入点。

DeltaNet 是 prefill 的主要计算瓶颈，其设备执行链路收敛为：

```text
packed QKV/Z/A/B projection
        ↓
causal conv1d + SiLU + conv state 原位更新
        ↓
Q/K 归一化与 α/β 因子
        ↓
gated-delta recurrence + FP32 recurrent state 原位更新
        ↓
gated RMSNorm → output projection
```

Causal conv kernel 分别针对 prefill 和单 token decode specialization：prefill 处理完整序列并保存末尾卷积状态，decode 直接滚动固定长度 conv state。Recurrent kernel 沿 value 维以 `block_v=8` 分块，每个程序实例在 token 维顺序完成递推，从设备执行路径中移除 Transformers fallback 的 Python chunk loop、clone/copy 和大量独立 reduction。Prefill 还为每个 `[batch, token, head]` 预计算 Q/K 范数倒数、状态衰减系数 `α` 和更新门 `β` 四个 FP32 因子，并在不同 value tile 间复用；单 token decode 则在 recurrent kernel 内直接计算，避免创建临时 tensor。

MLP 根据 shape 采用两条路径。Prefill 先执行 packed gate/up `F.linear`，再由一个 kernel 完成 SiLU 和乘法；固定 shape `[1,1,2048]` 的 decode 使用 PPU 专用 fused SwiGLU GEMV：

```text
hidden[1,1,2048] × packed_weight[12288,2048]
        ↓（FP32 accumulator，BF16 projection rounding）
gate / up + SiLU + multiply 保持在寄存器中
        ↓
activated[1,1,6144] → down projection
```

该 kernel 使用 `block_n=8`、`block_k=128`、4 warps 和单 stage。它不改变 GEMV 的并行计算方式，主要收益来自把 gate/up 投影后的舍入、SiLU 和乘法留在同一个 kernel 内，不再分别落盘两个 BF16 中间张量。

Full Attention 在 packed QGKV projection 后，由一个 `qgkv_norm_rope` kernel 完成 Q/gate/K/V 拆分、Q/K 的 FP32 RMSNorm、前 64 维多模态 RoPE 和 Attention 布局生成；decode 时由该 kernel 将 K/V 直接写入静态 cache 的当前位置。Attention 主体继续使用 Transformers attention interface，输出经过 fused sigmoid gate 后进入 O projection。Vision 采用对应的 packed QKV 后处理 kernel，并融合位置 embedding 插值求和、hidden add 以及每层 residual-add/LayerNorm 边界。

LM Head 不再生成完整的 BF16 logits `[1,248320]`。`lm_head_argmax` 以 `block_n=16` 执行分块 GEMV，FP32 accumulator 先按 eager 路径舍入为 BF16，每块只写出一个局部最大值及其词表索引，随后一次全局归约得到 Top-1；分数相同时选择较小的词表索引。该 kernel 同时用于 prefill 的首 token 和 decode graph 内的后续 token。

### 3.3 系统优化

系统优化负责将单个 kernel 的收益扩展到完整请求，重点处理 CPU 元数据准备、设备状态复用、Decode Graph 和部署预编译。

Vision 所需的 interpolation indices/weights、position ids、`cu_seqlens` 和 `max_seqlen` 在 CPU processor 阶段一次性计算，并随模型输入一起搬移到 PPU。24 层 Vision block 直接复用这些结果，不再逐层重新计算、从设备回读或触发额外同步。

Decode 使用 StaticCache 和长度分桶。请求所需容量按 `input_len + max_new_tokens + 1` 计算，向上取整到 128 的倍数，最小 bucket 为 512。每个 bucket 持久化保存：

- Full Attention KV cache、Delta conv state 和 recurrent state；
- 固定地址的 token、position 和输出 buffer；
- 一张绑定上述地址的 `GreedyDecodeGraph`。

新请求先选择能够容纳自身的最小 bucket，重置其中的 cache/state，执行 prefill 并更新静态 token/position buffer，随后直接 replay 已捕获的 graph，不再为每个请求重新分配 cache 或 capture graph。公开集实际使用 512、640、768、896、1024 五档，其中 512 和 640 覆盖约 97% 的样本。分桶既保持 graph 所需的固定地址，又避免所有请求统一使用最大 cache。

`GreedyDecodeGraph` 覆盖模型单 token decode、LM-head Top-1、token feedback 和 position increment。Host 侧每生成一个 token 只提交一次 graph replay 并检查 EOS，将融合后仍存在的数百次 kernel launch 从逐算子提交改为一次图提交。`QWEN35_GRAPH_STEPS` 支持在一张 graph 中串联多个 token；当前全量结果使用默认值 1，以便每步生成后立即检查 EOS。

部署阶段由 `precompile_triton.py` 为常用 prefill/decode shape、cache write 和 Vision shape 生成 PPU Triton 缓存，并写入仓库的 `triton/` 目录。运行时优先加载预编译结果，避免首个请求承担编译成本；`QWEN35_FUSIONS=0` 可关闭融合路径用于基线对照，模型权重文件和 benchmark 入口无需修改。

## 4. 优化前后评价指标对比与性能总结

### 4.1 评测口径

最终对比采用同目录下的全量结果：

- 原始基线：`baseline_full_en.json`、`baseline_full_cn.json`；
- 当前优化：`fused_full_v2_en.json`、`fused_full_v2_cn.json`；
- 每个语言集 4029 题，共 8058 题；
- 随机种子均为 20260625，BF16、batch size 1、greedy、最多生成 256 token；
- 性能百分比使用结果文件中已汇总的指标计算，双语平均因样本数相同而取算术平均；完整运行时间取两个数据集 wall time 之和。

### 4.2 全量数据集结果

| 数据集 | 指标 | Transformers BF16 eager | 当前优化 | 变化 |
|---|---|---:|---:|---:|
| English | Accuracy | 79.7220%（3212/4029） | 79.8213%（3216/4029） | +0.0993 个百分点 |
| English | Avg TTFT | 165.022 ms | 25.733 ms | -84.41% |
| English | Avg Throughput | 65.211 token/s | 240.918 token/s | +269.44%，3.694× |
| English | 完整运行时间 | 1812.969 s | 593.079 s | -67.29%，3.057× |
| Chinese | Accuracy | 84.1152%（3389/4029） | 83.8918%（3380/4029） | -0.2234 个百分点 |
| Chinese | Avg TTFT | 122.996 ms | 25.988 ms | -78.87% |
| Chinese | Avg Throughput | 46.396 token/s | 241.102 token/s | +419.66%，5.197× |
| Chinese | 完整运行时间 | 4724.945 s | 1146.271 s | -75.74%，4.122× |
| 双语合并 | Accuracy | 81.9186%（6601/8058） | 81.8565%（6596/8058） | -0.0621 个百分点 |
| 双语合并 | Avg TTFT | 144.009 ms | 25.861 ms | -82.04% |
| 双语合并 | Avg Throughput | 55.804 token/s | 241.010 token/s | +331.89%，4.319× |
| 双语合并 | 完整运行时间 | 6537.914 s | 1739.350 s | -73.40%，3.759× |

### 4.3 性能总结

DeltaNet 融合主导 prefill 收益，Decode Graph 主导逐 token 吞吐收益，权重生命周期管理则消除了 packing 带来的显存冗余。全量结果中双语准确率基本持平，TTFT、吞吐和完整运行时间均得到显著改善。当前实现面向 PPU-ZW810E 上的 BF16、batch size 1、greedy 单图问答推理。
