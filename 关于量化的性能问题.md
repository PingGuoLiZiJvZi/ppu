# 关于当前运行时量化的性能问题

## 1. 结论摘要

当前实验环境中，TorchAO 的 W8A16 和 W8A8 都没有表现出相对 BF16 eager 的速度优势。核心原因不是 A100 缺少 INT8 算力，而是当前工作负载和执行路径下，低精度 GEMM 节省的时间小于量化、缩放、张量处理和编译调用产生的额外开销。

当前证据支持以下判断：

- BF16 baseline 使用 A100 上高度优化的 BF16 Tensor Core GEMM；项目中的典型单 Linear 延迟约为 `0.04–0.07 ms`。
- W8A16 的当前 TorchAO 路径不是直接执行 BF16 激活乘 INT8 权重的低精度 GEMM，而是在 forward 中将 INT8 权重转换为 BF16，再执行 BF16 GEMM 并缩放。因此它保留了 BF16 GEMM，又增加了权重转换和缩放开销。
- W8A8 确实执行动态激活 INT8 × 权重 INT8 的 GEMM，但每次 forward 还要完成激活统计、动态量化、INT32 输出处理和 scale 恢复。Inductor 没有把整个过程融合成单一 kernel。
- 本项目是 batch size 1，decode 阶段主要是 `M=1` 的 GEMM；输出通常只有几到几十个 token。此时单次 BF16 GEMM 已经很短，INT8 的固定开销难以摊销。
- `torch.compile` 能显著改善整模型 W8A8 eager 的性能，但在已测试的真实样本上仍未超过 BF16 eager。

这些结果目前属于微基准、profiler 和真实样本 sanity check，不是完整数据集上的正式性能结论。

## 2. 实验环境与运行条件

诊断时使用的主要环境为：

| 项目 | 配置 |
| --- | --- |
| GPU | NVIDIA A100-SXM4-40GB |
| PyTorch | `2.13.0+cu130` |
| CUDA runtime | `13.0` |
| TorchAO | `0.18.0` |
| Transformers | `5.15.0` |
| 量化实验设备 | `cuda:1` |
| BF16 baseline 设备 | `cuda:0` |
| batch size | 1 |
| 生成上限 | `max_new_tokens=256` |
| 实际输出长度 | 通常为几到几十个 token |
| 编译后端 | Inductor |
| 测试过的编译配置 | `mode="max-autotune"`、`dynamic=True` 等 |

现有 BF16 baseline 没有启用 `torch.compile`，属于 eager 路径。量化探索脚本后来加入了编译流程，因此量化结果与已有 BF16 baseline 的速度对比不是严格的同后端对比；已有 baseline 仍可用于准确率门禁。

## 3. 当前量化实现的实际执行路径

### 3.1 W8A16

W8A16 使用：

```python
Int8WeightOnlyConfig()
```

权重以 INT8 形式存储，激活保持 BF16。但当前 TorchAO `Int8Tensor` 的 per-row weight-only forward 路径会将权重值转换到激活 dtype，再执行 BF16 `torch.mm`，最后乘权重 scale。它并不是本方案原本期望的原生低精度混合 GEMM。

执行过程可概括为：

```text
INT8 weight → 转换为 BF16 → BF16 GEMM → 乘 weight scale
```

因此，W8A16 当前主要提供权重存储压缩和精度敏感性测试价值，而不是计算加速。它比 BF16 dense 慢是当前实现路径的直接结果。

### 3.2 W8A8

W8A8 使用：

```python
Int8DynamicActivationInt8WeightConfig()
```

默认方案是动态、对称、per-row 激活量化以及 per-row（对 Linear 权重即 per-output-channel）权重量化。每次 Linear forward 包含：

```text
计算激活量化参数
→ BF16 activation 量化为 INT8
→ INT8 × INT8 GEMM，累加为 INT32
→ 应用 activation scale
→ 应用 weight scale
→ 转换为 BF16 输出
```

TorchAO 文档和源码将该配置描述为需要 `torch.compile` 优化的 INT8 MM 路径。仅调用 `quantize_()` 不会自动编译模型；如果不显式调用 `torch.compile`，它会在 eager 模式下逐操作执行。

## 4. 微基准观察

### 4.1 最初的 eager 结果

最初未编译时得到：

```text
W8A16 speedup = 0.348x–0.530x
W8A8  speedup = 0.026x–0.052x
```

其中 speedup 定义为：

```text
BF16 median latency / quantized median latency
```

因此小于 1 表示量化更慢。W8A8 eager 的单 Linear 延迟约为 `0.95–1.63 ms`，BF16 dense 约为 `0.04–0.07 ms`。这说明 eager W8A8 后端不适合当前形状，但不能据此判断编译后的 W8A8 性能。

### 4.2 首次加入 compile 后的无效结果

首次给微基准加入 `torch.compile` 后输出：

```text
W8A16 speedup = 0.012x–0.307x
W8A8  speedup = 0.023x–0.046x
```

这批结果无效，不能用于比较。日志明确显示：

```text
torch._dynamo hit config.recompile_limit (8)
```

微基准连续创建了多个不同参数形状的模块，但它们命中了同一个 Dynamo frame。第 9 个形状开始超过默认重编译上限，后续调用回退到 eager。由于 W8A16 排在前面、W8A8 排在后面，W8A8 的 24 组基本都没有真正走编译路径。

脚本后来在每个形状编译前加入：

```python
torch.compiler.reset()
```

以隔离不同形状的编译状态。不过完整的修正后 48 组结果尚未形成正式记录，因此现有 `microbenchmark.json` 不能作为正式证据。

### 4.3 独立 W8A8 compile 结果

在隔离编译上下文后，以 `2048 → 6144` 的 Linear 测试：

| token 数 M | BF16 eager | W8A8 compile | speedup |
| ---: | ---: | ---: | ---: |
| 1 | 约 `0.042 ms` | 约 `0.157 ms` | 约 `0.27x` |
| 128 | 约 `0.050 ms` | 约 `0.170 ms` | 约 `0.30x` |

这说明修正后的 W8A8 compile 明显优于 W8A8 eager，但单 Linear 仍比 BF16 eager 慢约 3.4–3.7 倍。

需要注意：单 Linear 结果不能直接外推到整模型。单 Linear 每次只执行一次 GEMM，却承担一次 compiled module 调用成本；整模型或较大的计算块可以在一次 compiled call 中执行多个 Linear，从而摊销部分开销。

## 5. 对慢速原因的逐项诊断

### 5.1 compile mode 与 dynamic 参数

测试了以下组合：

- `mode="default"`，`dynamic=False/True`
- `mode="reduce-overhead"`，`dynamic=False/True`
- `mode="max-autotune"`，`dynamic=False/True`

对 `2048 → 6144` W8A8 Linear，能够运行的组合基本都落在 `0.155–0.168 ms`。因此当前慢速不是简单选择了错误的 `mode` 或 `dynamic` 参数。

另外，`default` 或 `reduce-overhead` 在 `M=1` 时可能选择外部 `_int_mm`，并报错：

```text
self.size(0) needs to be greater than 16, but got 1
```

`max-autotune` 会忽略这个失败候选并选择可运行的 Triton INT8 kernel。因此，当前 `M=1` decode 不能直接切换到不带 autotune 的静态编译配置。

### 5.2 动态 scale 是否是主要瓶颈

对比了：

- 动态 per-row 激活量化
- 动态 per-tensor 激活量化
- 固定 scale 的静态 per-tensor 激活量化

结果都约为 `0.15–0.18 ms`，差别很小。由此可知，`amax`/scale 统计不是全部瓶颈；即使预先固定激活 scale，当前 TorchAO/Inductor 图仍有量化、类型转换、输出缩放和调用开销。

因此不能通过“把动态 W8A8 改成静态 W8A8”直接解决性能问题。静态 W8A8 还需要定义校准集、校准方法和 scale 规则，会改变实验方案，不能未经批准替换。

### 5.3 INT8 GEMM 本身是否慢

Inductor autotune 日志显示，在 `M=128, K=2048, N=6144` 时，最快 INT8 GEMM kernel 约为 `0.03–0.04 ms`。它并没有比完整 W8A8 Linear 的 `0.16–0.17 ms` 慢。

这说明主要损失发生在完整 W8A8 计算图的其他部分，而不是 A100 的 INT8 Tensor Core 完全没有发挥作用。

### 5.4 profiler 结果

对编译后的 W8A8 Linear 进行 profiler sanity check，观察到主要 GPU 工作包括：

- 激活 `amax/amin` 和量化相关的 reduction kernel
- clamp/div/cast 等 pointwise kernel
- INT8 MM 与一部分缩放融合后的 Triton kernel
- CUDA Graph 输入复制相关的 `aten::_foreach_copy_`
- compiled graph/AOT runtime wrapper 的调用开销

Profiler 中一次计算图内部的 CUDA 工作约为几十微秒，但端到端单模块调用约为 `0.15–0.17 ms`。两者差值表明，编译包装、输入复制、量化前后处理和 kernel 启动在这种小矩阵场景中占据显著比例。

### 5.5 `Int8Tensor` 是否是唯一原因

曾怀疑 TorchAO `Int8Tensor` 张量子类的 AOT wrapper 和缓存处理是主要原因。为验证这一点，使用普通 INT8 buffer 写了等价原型：

```text
动态量化 activation
→ torch._int_mm
→ activation/weight scale 恢复
```

原型在 `M=1/128/1024` 上仍然慢于 BF16，约为：

| M | BF16 eager | 普通 INT8 buffer compile | speedup |
| ---: | ---: | ---: | ---: |
| 1 | `0.040 ms` | `0.156 ms` | `0.26x` |
| 128 | `0.048 ms` | `0.171 ms` | `0.28x` |
| 1024 | `0.132 ms` | `0.201 ms` | `0.66x` |

所以 `Int8Tensor` 不是唯一根因。即便绕开张量子类，当前小批量计算中动态量化、缩放、编译调用和多个 kernel 的总成本仍大于 BF16 GEMM。随着 M 增大，差距在缩小，这与固定开销逐渐被更大 GEMM 摊销一致。

## 6. 真实样本 sanity check

使用同一个英文真实样本、相同生成配置测试了 `W8A8_Q_1000`，即只量化语言 MLP。

### 6.1 W8A8 eager

三次测量约为：

```text
TTFT：601–624 ms
36 token 总耗时：4.24–4.50 s
```

中位总耗时约 `4.25 s`。

### 6.2 仅编译每层 MLP 计算块

将每个语言层的整个 MLP 作为一个编译边界，使 gate/up/down 三个量化 Linear 共享一次 compiled module 调用。结果约为：

```text
首次编译预热：24.2 s
TTFT 中位数：457 ms
36 token 总耗时中位数：1.89 s
```

相对 W8A8 eager，MLP 块级编译使总耗时降低约 55%，证明编译边界和调用摊销非常重要。

### 6.3 BF16 eager 对照

同一真实样本的 BF16 eager 结果约为：

```text
TTFT 中位数：318 ms
36 token 总耗时中位数：1.57 s
```

因此，即使采用有效的 MLP 块级编译，W8A8 在该样本上仍比 BF16 eager 总耗时慢约 21%，TTFT 慢约 44%。这只是单样本 sanity check，不能作为完整数据集上的正式结论，但足以证明“加上 compile 就一定能加速”不成立。

## 7. 编译边界问题

### 7.1 直接编译整个多模态模型

当前量化探索脚本使用：

```python
model._model.compile(mode="max-autotune", dynamic=True)
```

真实样本测试中，Qwen3.5 多模态顶层 forward 在图像占位符检查等位置发生 graph break。Inductor 将模型拆成大量子图并分别 autotune，首次编译超过 4 分钟仍未完成，之后手动终止。

因此，直接编译整个 VLM 不是当前可接受的正式探索流程。它会导致每个候选初始化成本失控，也可能让预热无法覆盖正式运行中的所有形状。

### 7.2 编译整个语言模型

还测试了只编译 `model.language_model`。该路径没有立即出现多模态顶层的 graph-break 警告，但首次编译超过 2 分钟仍未完成，测试随后被中断。当前证据不足以评价它的稳定态性能，但它不适合直接投入 30 个候选的探索运行。

### 7.3 编译 MLP 块

MLP 块级编译已成功完成，并取得显著的端到端改善；它是目前唯一得到真实样本验证的编译边界。但当前量化组合还包含 F、L、V，若正式采用块级编译，需要分别定义：

- M：编译整层 MLP
- F：编译全注意力计算块或其合适子块
- L：编译线性注意力计算块或其合适子块
- V：编译视觉 block 或其合适子块

这些边界尚未全部验证，不能将 MLP 的结果直接外推到 F/L/V。

## 8. 当前数据与结果的有效性

### 可以作为诊断证据的内容

- TorchAO 源码中 W8A16 和 W8A8 的实际 forward 路径。
- 隔离上下文后的单 Linear W8A8 compile 结果。
- 多种 compile mode/dynamic 配置的对比。
- 动态/静态、per-row/per-tensor 激活量化的对比。
- profiler 中的 kernel 类型和调用结构。
- 同一真实样本上 BF16 eager、W8A8 eager 和 MLP 块级 W8A8 compile 的对比。

### 不能作为正式结论的内容

- 触发 `recompile_limit=8` 后生成的 48 组微基准结果。
- 被手动中断的整模型和语言模型编译测试。
- 用一个真实样本得出的全数据集速度或准确率结论。
- 将 MLP 块级结果直接推广到 F、L、V 组合。

## 9. 当前脚本存在的风险

当前 `explore_runtime_quantization.py` 已加入：

```python
model._model.compile(mode="max-autotune", dynamic=True)
```

但真实样本诊断已经表明，直接编译整个 VLM 会发生大量 graph break，首次编译成本不可接受。因此当前正式量化探索流程不应继续依赖这一整模型编译方式。

微基准已加入不同形状之间的 `torch.compiler.reset()`，但必须重新完整运行并确认日志中不再出现 `recompile_limit`，才能生成新的有效结果。

## 10. 对实验决策的含义

当前证据支持以下决策：

1. 不能把现有 TorchAO W8A16 当作加速后端；它更适合作为权重压缩和精度敏感性路径。
2. W8A8 eager 不适合当前 batch=1、短输出工作负载。
3. W8A8 compile 的性能高度依赖编译边界；整模型边界不可用，单 Linear 边界开销过大，MLP 块级边界有效但仍未超过 BF16。
4. 在没有完成 F/L/V 块级编译验证前，不能开始 30 个候选的正式筛选并声称它们代表优化后的 W8A8 后端。
5. 即便采用块级编译，短 decode 的 `M=1` 仍可能使 W8A8 组合在端到端速度上落后；这应由完整的 64 条筛选决定，而不是由单 Linear 微基准直接决定。

## 11. 后续需要完成的工作

若继续使用 TorchAO W8A8，合理顺序是：

1. 移除当前整模型 compile 边界。
2. 为 M/F/L/V 分别确定可运行的计算块编译边界。
3. 每类先做一个真实样本 smoke test，确认能够完成编译和生成。
4. 再做固定小样本 sanity check，记录首次编译时间、是否重编译、TTFT 和吞吐。
5. 只有在编译边界冻结后，才重跑 64 条筛选。
6. 保留 BF16 eager baseline 用于准确率门禁；若要做严格同后端速度归因，应额外运行相同编译边界的 BF16 对照，而不是覆盖已有 baseline。

如果块级 W8A8 在 64 条阶段仍普遍慢于 BF16，并且不会改变候选选择，则应将当前 TorchAO W8A8 后端判定为不适合本工作负载，而不是继续通过调整实验参数追求表面加速。

## 12. 证据状态

截至目前：

- 已解释 W8A16/W8A8 为什么在当前路径下慢。
- 已确认 `torch.compile` 可以改善 W8A8，但不能保证超过 BF16。
- 已发现并修复微基准的跨形状重编译上限问题，但尚未取得新的完整有效结果。
- 已确认当前整模型编译流程不适合正式探索。
- 尚未完成所有 M/F/L/V 编译边界的实现与验证。
- 尚无足够证据宣称运行时量化能提高该模型在 A100 上的端到端性能。
