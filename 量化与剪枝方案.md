# Qwen3.5-2B 运行时量化与剪枝技术探索方案

## 1. 目标与约束

本方案探索在不提交、不持久化修改后模型权重的前提下，通过运行时量化和运行时剪枝降低 Qwen3.5-2B 多模态推理的 TTFT、单 token 解码延迟和峰值显存，并尽可能提高吞吐。

核心约束如下：

- 启动时始终从主办方提供的原始模型目录加载权重。
- 量化、稀疏化、mask、模块替换和缓存转换只发生在内存中。
- 优化版本的准确率相对同条件 baseline 最多下降 **2 个百分点**：

  ```text
  accuracy_candidate >= accuracy_baseline - 0.02
  ```

## 3. 总体原则

### 3.1 先测量，再改变计算路径

正式 baseline 对每条样本记录：

- 图片 Base64 解码和 Pillow 转换时间；
- processor/chat template 时间；
- CPU 到设备的数据传输时间；
- vision encoder GPU 时间；
- language model prefill GPU 时间；
- TTFT；
- decode 总时间、生成 token 数和平均每 token 时间；
- 输入 token 数和视觉 token 数；
- CUDA 峰值 allocated 显存和峰值 reserved 显存。

CPU 阶段使用 `perf_counter`，GPU 阶段使用 CUDA Event。测量代码不得启用 PyTorch Profiler、逐层 hook、kernel trace 或其他会显著改变 TTFT 和吞吐的诊断功能。


## 4. 准确率门禁设计

每个候选方案必须满足：

```text
candidate_accuracy >= baseline_accuracy - 0.02
```


## 5. 运行时量化路线

### 5.1 量化边界与实现原则

当前模型权重为 BF16，语言侧包含 24 层，其中 18 层为线性注意力、6 层为全注意力；视觉侧包含 24 层视觉编码器。首轮采用**权重量化、激活保持 BF16**，并在模型初始化阶段完成 scale 计算和权重 pack。

四类量化模块记为 `M/F/L/V`：

| 代号 | 范围 | 原因 |
| --- | --- | --- |
| M | `language_model.layers.*.mlp.{gate,up,down}_proj` | 参数量和计算量大，同时影响 prefill 与 decode |
| F | 全注意力的 `q/k/v/o_proj` | 降低全注意力投影的权重带宽 |
| L | 线性注意力的输入/输出投影 | 降低线性注意力矩阵投影的权重带宽 |
| V | `visual.blocks.*` 内的大型 Linear | 改善视觉编码与 TTFT |

scale、zero-point 和 packed weight 从原始权重即时计算并保留在内存中。优先选择能够直接执行低精度 GEMM 的后端；逐次反量化再执行 BF16 GEMM 的实现用于精度敏感性分析。额外依赖及版本写入 `requirements_extra.txt`。

### 5.2 组合候选

四类模块分别作为独立开关。排除已完成的全 BF16 baseline 后，共有 `2^4 - 1 = 15` 个量化组合。编号 `Q_MFLV` 的四位依次表示 M、F、L、V 是否启用 W8A16；`1` 表示启用，`0` 表示保持 BF16。

| 配置 | M | F | L | V | 量化范围 |
| --- | ---: | ---: | ---: | ---: | --- |
| Q_1000 | 1 | 0 | 0 | 0 | M |
| Q_0100 | 0 | 1 | 0 | 0 | F |
| Q_0010 | 0 | 0 | 1 | 0 | L |
| Q_0001 | 0 | 0 | 0 | 1 | V |
| Q_1100 | 1 | 1 | 0 | 0 | M + F |
| Q_1010 | 1 | 0 | 1 | 0 | M + L |
| Q_1001 | 1 | 0 | 0 | 1 | M + V |
| Q_0110 | 0 | 1 | 1 | 0 | F + L |
| Q_0101 | 0 | 1 | 0 | 1 | F + V |
| Q_0011 | 0 | 0 | 1 | 1 | L + V |
| Q_1110 | 1 | 1 | 1 | 0 | M + F + L |
| Q_1101 | 1 | 1 | 0 | 1 | M + F + V |
| Q_1011 | 1 | 0 | 1 | 1 | M + L + V |
| Q_0111 | 0 | 1 | 1 | 1 | F + L + V |
| Q_1111 | 1 | 1 | 1 | 1 | M + F + L + V |

所有启用模块统一采用对称、per-output-channel 的 W8A16 weight-only 量化。已完成的 BF16 baseline 不重复运行，其结果只用于准确率门禁和 baseline 参考分。

### 5.3 量化实验流程

1. **量化 kernel 预检与微基准**：语言 MLP、全注意力和线性注意力使用模型实际尺寸测试典型 prefill 与 `M=1` decode，视觉 Linear 使用代表性视觉 token 尺寸测试 encoder forward；确认 W8A16 的实际执行路径，并与 BF16 的中位延迟和 P90 延迟比较。
2. **64 条初步筛选**：15 个量化组合在固定双语 64 条样本上运行。64 条由英文 32 条和中文 32 条组成，并按题型、图片尺寸和 prompt 长度分层抽取。使用双语合并指标计算 `FinalScore_max_ref`，保留排名前 6 个组合。
3. **256 条探索筛选**：前 6 个组合在固定双语 256 条样本上运行。256 条由英文 128 条和中文 128 条组成。使用已有 baseline 在相同样本上的准确率执行第 4.2 节门禁，再按双语 `FinalScore_max_ref` 排名，保留前 3 个组合。
4. **完整运行**：冻结前 3 个组合的量化范围、dtype、scale 计算方式和 generation config，在完整数据集上运行。使用已有 baseline 在相同样本上的准确率执行第 4.2 节门禁，再对通过门禁的量化组合计算双语 `FinalScore_max_ref` 并排序。

每一阶段都将英文和中文记录合并后计算 Accuracy、Avg TTFT 和 Avg Throughput：

```text
FinalScore_max_ref = (Accuracy - min(Accuracy)) / (max(Accuracy) - min(Accuracy)) * 4
                   - AvgTTFT / min(AvgTTFT) * 3
                   + AvgThroughput / max(AvgThroughput) * 3
```

其中 `max/min` 只在该阶段参评的量化组合内部计算，不包含 baseline；完整验收阶段只纳入通过准确率门禁的量化组合。若分数相同，依次比较 Accuracy、Avg TTFT 和 Avg Throughput。

64 条筛选各运行 3 次并使用中位数，256 条筛选各运行 2 次并使用平均数；完整验收运行 1 次。除 Avg TTFT、Avg Throughput 外，记录模型加载时间、量化/pack 时间、峰值 CPU/GPU 内存以及量化模块数量。

## 6. 运行时剪枝路线
在量化实验的四个最佳候选上进行
### 6.1 剪枝收益判定

普通 dense Tensor 中把权重置零不会减少 GEMM FLOPs。剪枝性能路线主要包括：

- 剪掉完整通道并同步缩小相邻矩阵，使 dense GEMM 的实际维度变小；
- 转换成半结构化稀疏格式并使用对应 sparse kernel；
- 减少进入后续视觉层的 token 数，实际降低视觉 encoder FLOPs。

mask、通道索引和稀疏 packed weight 在启动时由原始权重计算，并使用固定剪枝配置。

### 6.2 候选路线

| 编号 | 路线 | 初始搜索范围 | 关键实现 |
| --- | --- | --- | --- |
| P0 | 语言 MLP 通道剪枝 | `ratio ∈ {5%, 10%, 15%, 20%}` | 对每层 SwiGLU 同步删除 `gate_proj/up_proj` 的输出行和 `down_proj` 的输入列，生成更小的连续矩阵 |
| P1 | 2:4 半结构化稀疏 | 先测语言 MLP 中间层，再扩展到投影层 | 每连续 4 个权重保留幅值最大的 2 个，使用原生 2:4 kernel |
| P2 | 视觉 token 剪枝 | `ratio ∈ {10%, 20%, 30%}`，从视觉中后层开始 | 按固定规则或当前隐藏状态的重要性保留 token，同步调整 position、grid、mask 和 merger |
| P3 | attention head / 整层剪枝 | 后续探索 | 单独处理 GQA、线性注意力状态和 cache 布局 |

P1 是首选剪枝路线。初始采用无需数据校准的权重范数评分；对 SwiGLU 第 `i` 个中间通道可使用下式排序：

```text
importance_i = sqrt(||gate_proj[i, :]||² + ||up_proj[i, :]||²)
             * ||down_proj[:, i]||
```

先做逐层 10% 通道剪枝，统计每层引起的答案翻转数，再对高敏感层降低比例或保留原结构。剪枝后的 intermediate size 对齐 kernel tile，例如 64 或 128 的倍数。

P2 先使用实际 shape 比较 BF16 sparse 与 BF16 dense kernel。P3 主要影响视觉编码和 TTFT，排在语言 MLP 路线之后，并优先采用保持规则网格的块级 token 保留方式。



